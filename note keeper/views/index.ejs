<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
      <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="main p-10 w-full min-h-screen bg-zinc-900">
        <div class="form text-white">
           <form action="/create" method="post">
             <input type="text" class="block w-full rounded-md outline-none bg-zinc-800 px-5 py-2" placeholder="Title" name="title">
            <textarea  class="block bg-zinc-800  w-full rounded-md outline-none mt-2 resie-none px-5 py-2" placeholder="write your tasks here" name="details"> </textarea>
            <input type="submit" class="mt-2 bg-blue-600 px-5 py-2 rounded-md " value="Add Task">
           </form>
        </div>


        <div class="tasks flex gap-4 flex-wrap p-10">
        <% if(files.length > 0){ %>
               
            <% files.forEach((ele)=>{ %>
                     <div class="task  rounded-md min-w-72 px-3 py-4 bg-zinc-800">
                <h1 class="text-white text-2xl tracking-tighter"><%= ele %></h1>
                <div class="flex justify-between w-full items-center">
                    
                <a class="text-blue-500 inline-block mt-3" href="/file/<%=ele%>">read more</a>
                <a class="text-zinc-600 inline-block mt-3" href="/edit/<%=ele%>">edit filename</a>
                </div>
            </div>
         <%   })%>

        <% } else { %>
            <h3 class="text-zinc-500 text-2xl">No tasks Yet</h3>
       <% } %>

        </div>
    </div>
</body>
</html>